language: node_js
sudo: false
node_js:
  - "node"
cache:
  directories:
    - "node_modules"
script:
  - npm run-script lint
  - npm run-script build
deploy:
  provider: script
  script: npx firebase deploy --token "$FIREBASE_TOKEN"
  on:
    branch: master
